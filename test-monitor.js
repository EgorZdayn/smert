/**
 * Тестовый файл для проверки работы монитора
 */

const VolumeMonitor = require('./volume-monitor');

// Тестовая конфигурация
const config = {
    symbols: ['STABLEUSDT', 'BTCUSDT'],
    volumeMultiplier: 1.5, // Более низкий порог для теста
    checkInterval: 30000,  // 30 секунд для быстрого теста
    historySize: 10,
    
    // Раскомментируйте для тестирования Telegram
    // telegramToken: 'YOUR_BOT_TOKEN',
    // telegramChatId: 'YOUR_CHAT_ID'
};

console.log('🧪 Запуск тестового мониторинга...\n');
console.log('⚠️  Это тестовый режим с пониженным порогом аномалии (x1.5)');
console.log('⚠️  Интервал проверки: 30 секунд\n');

const monitor = new VolumeMonitor(config);

// Запускаем мониторинг
monitor.start();

// Автоматическая остановка через 5 минут
setTimeout(() => {
    console.log('\n\n✅ Тест завершен (5 минут прошло)');
    process.exit(0);
}, 5 * 60 * 1000);
