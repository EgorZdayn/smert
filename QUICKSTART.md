# Быстрый старт 🚀

## Установка (2 минуты)

```bash
# 1. Установите зависимости
npm install

# 2. Создайте конфигурацию
cp config.example.js config.js

# 3. Отредактируйте config.js
# - Добавьте свои монеты в symbols
# - Настройте порог аномалии (volumeMultiplier)
# - (опционально) Добавьте Telegram токен и chat_id
```

## Использование

### Найти интересные монеты для мониторинга
```bash
npm run find              # Рекомендации
npm run find:lowcap       # Низкокапитализированные
npm run find:volatile     # Высоковолатильные
npm run find:top          # Топ по объему
```

### Запустить мониторинг
```bash
npm start                 # С config.js
npm run monitor          # Напрямую
npm test                 # Тестовый режим (5 минут)
```

## Пример config.js

```javascript
module.exports = {
    symbols: [
        'STABLEUSDT',
        'PEPUSDT',
        'SHIBUSDT',
    ],
    volumeMultiplier: 2.0,
    checkInterval: 60000,
    historySize: 24,
    
    // Telegram (опционально)
    telegramToken: null,
    telegramChatId: null,
};
```

## Настройка Telegram (опционально)

1. Создайте бота у [@BotFather](https://t.me/BotFather)
2. Получите chat_id у [@userinfobot](https://t.me/userinfobot)
3. Добавьте в config.js:
   ```javascript
   telegramToken: 'YOUR_BOT_TOKEN',
   telegramChatId: 'YOUR_CHAT_ID',
   ```

## Что дальше?

Читайте полный [README.md](README.md) для:
- Детальной настройки
- Запуска в фоновом режиме
- Стратегий использования
- Решения проблем

---

**Готово! Ваш мониторинг работает 24/7** 🎉
