# 🎉 Обновление: теперь с поддержкой FUTURES!

## ✨ Что нового в v2.0?

✅ **Поддержка SPOT** (спот)  
✅ **Поддержка FUTURES** (фьючерсы)  
✅ **Автоматическое определение** типа рынка  
✅ **Единый мониторинг** для обоих типов  

---

## 🚀 Быстрый старт

### 1. Используйте новый файл

**Вместо:**
```bash
node volume-monitor.js  # старая версия (только spot)
```

**Используйте:**
```bash
node volume-monitor-v2.js  # новая версия (spot + futures)
```

### 2. Обновите config.js

Добавьте фьючерсные пары (с подчеркиванием `_`):

```javascript
symbols: [
    // SPOT пары (без подчеркивания)
    'BTCUSDT',
    'ETHUSDT',
    'STABLEUSDT',
    
    // FUTURES пары (с подчеркиванием)
    'BTC_USDT',
    'ETH_USDT',
],
```

---

## 🔍 Найти интересные пары

### Для SPOT:
```bash
npm run find              # Рекомендации
npm run find:lowcap       # Низкокапитализированные
npm run find:volatile     # Волатильные
npm run find:top          # Топ по объему
```

### Для FUTURES:
```bash
npm run find:futures            # Рекомендации
npm run find:futures:lowcap     # Низкокапитализированные
npm run find:futures:volatile   # Волатильные
npm run find:futures:top        # Топ по объему
```

---

## 📝 Различия форматов

| Тип | Формат | Пример | URL |
|-----|--------|--------|-----|
| **SPOT** | Без `_` | `BTCUSDT` | mexc.com/exchange/BTCUSDT |
| **FUTURES** | С `_` | `BTC_USDT` | futures.mexc.com/exchange/BTC_USDT |

**⚠️ Не путайте!** `BTCUSDT` ≠ `BTC_USDT`

---

## 💡 Примеры использования

### Только спот (как раньше):
```javascript
symbols: ['BTCUSDT', 'ETHUSDT']
```

### Только фьючерсы:
```javascript
symbols: ['BTC_USDT', 'ETH_USDT']
```

### Комбинация:
```javascript
symbols: [
    'BTCUSDT',    // Spot
    'BTC_USDT',   // Futures
]
```

---

## 📚 Документация

- **SPOT_VS_FUTURES.md** - подробное руководство
- **config-v2.js** - пример конфигурации
- **volume-monitor-v2.js** - новый мониторинг

---

## ⚡ Команды

```bash
# Запуск новой версии
npm run monitor:v2

# Или напрямую
node volume-monitor-v2.js

# Поиск futures пар
npm run find:futures
```

---

**Готово! Теперь можете мониторить и спот, и фьючерсы! 🎉**
