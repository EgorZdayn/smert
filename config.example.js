// config.example.js - Пример конфигурации

export const config = {
  // ═══════════════════════════════════════════════════════════════
  // 📊 МОНИТОРИНГ МОНЕТ
  // ═══════════════════════════════════════════════════════════════
  
  // Список монет для отслеживания
  // Формат: 'БАЗОВАЯ_ВАЛЮТАТОРГОВАЯ_ВАЛЮТА' (например, 'BTCUSDT')
  symbols: [
    'STABLEUSDT',     // Основная монета
    // 'BTCUSDT',     // Bitcoin
    // 'ETHUSDT',     // Ethereum
    // 'BNBUSDT',     // Binance Coin
    // Добавьте сюда любые монеты, которые хотите мониторить
  ],

  // ═══════════════════════════════════════════════════════════════
  // 🔥 ПАРАМЕТРЫ ОБНАРУЖЕНИЯ АНОМАЛИЙ
  // ═══════════════════════════════════════════════════════════════
  
  // Множитель объема для срабатывания алерта
  // 2.0 = объем должен быть в 2 раза больше среднего
  // 3.0 = в 3 раза больше (более консервативно)
  // 1.5 = в 1.5 раза больше (более агрессивно)
  volumeMultiplier: 2.0,
  
  // ═══════════════════════════════════════════════════════════════
  // ⏱️ ВРЕМЕННЫЕ ПАРАМЕТРЫ
  // ═══════════════════════════════════════════════════════════════
  
  // Интервал проверки в миллисекундах
  // 30000  = 30 секунд (частые проверки)
  // 60000  = 1 минута (рекомендуется)
  // 120000 = 2 минуты (редкие проверки)
  checkInterval: 60000,
  
  // Период для расчета среднего объема (в количестве проверок)
  // При checkInterval = 60000 (1 минута):
  //   12 = 12 минут истории
  //   24 = 24 минуты истории (рекомендуется)
  //   60 = 1 час истории
  lookbackPeriod: 24,
  
  // ═══════════════════════════════════════════════════════════════
  // 📱 TELEGRAM УВЕДОМЛЕНИЯ
  // ═══════════════════════════════════════════════════════════════
  
  // Токен бота (получите у @BotFather)
  telegramToken: process.env.TELEGRAM_BOT_TOKEN,
  
  // ID чата (получите у @userinfobot)
  telegramChatId: process.env.TELEGRAM_CHAT_ID,
};

// ═══════════════════════════════════════════════════════════════
// 💡 ГОТОВЫЕ ПРЕСЕТЫ
// ═══════════════════════════════════════════════════════════════

// Агрессивный (ловит все небольшие скачки)
export const AGGRESSIVE_PRESET = {
  volumeMultiplier: 1.5,
  checkInterval: 30000,
  lookbackPeriod: 12,
};

// Сбалансированный (рекомендуется)
export const BALANCED_PRESET = {
  volumeMultiplier: 2.0,
  checkInterval: 60000,
  lookbackPeriod: 24,
};

// Консервативный (только крупные переливы)
export const CONSERVATIVE_PRESET = {
  volumeMultiplier: 3.0,
  checkInterval: 120000,
  lookbackPeriod: 30,
};

// Чтобы использовать пресет, раскомментируйте:
// export const config = { ...BALANCED_PRESET, symbols: ['STABLEUSDT'], ... };
